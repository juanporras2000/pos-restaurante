/**
 * colors.js — constantes hex pa librerías que no leen clases Tailwind
 * (ej: SweetAlert2 `confirmButtonColor` necesita un hex crudo).
 * Unifica los 3 rojos "destructivo" que convivían sueltos en el proyecto
 * (#dc2626, #d33) y el azul de marca usado en tailwind.config.js (primary).
 */
export const DANGER = '#dc2626';
export const NEUTRAL = '#6b7280';
export const PRIMARY = '#1B66FA';
